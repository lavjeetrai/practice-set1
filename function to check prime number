#include <iostream>
using namespace std;
char checkPrime(int n) {
	int count = 0;
	for (int i = 1; i <= n; i++) {
		if (n % i == 0) {
			count++;
		}
	}
		if (count == 2) {
			cout << "PRIME";
	}
		else {
			cout << "NOT PRIME";
		}
	return 0;
}
int main() {
	checkPrime(4);
	return 0;
}
